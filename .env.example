PORT=8787
ERP_COMMS_API_KEY=change-me
EMAIL_FROM=Asoltu School ERP <noreply@asoltuschool.com>

# Option A — SendGrid
SENDGRID_API_KEY=

# Option B — SMTP (Gmail app password, SES, Mailgun, etc.)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_SECURE=false

# FCM (Firebase Admin JSON, base64). Never commit the raw key.
FIREBASE_SERVICE_ACCOUNT_B64=
